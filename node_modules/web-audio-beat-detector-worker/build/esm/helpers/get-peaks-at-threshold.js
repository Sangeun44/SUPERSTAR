export var getPeaksAtThreshold = function (channelData, threshold, sampleRate) {
    var length = channelData.length;
    var peaks = [];
    var lastValueWasAboveThrehold = false;
    for (var i = 0; i < length; i += 1) {
        if (channelData[i] > threshold) {
            lastValueWasAboveThrehold = true;
        }
        else if (lastValueWasAboveThrehold) {
            lastValueWasAboveThrehold = false;
            peaks.push(i - 1);
            // Skip 0.25 seconds forward to get past this peak.
            i += (sampleRate / 4) - 1;
        }
    }
    // Add the last value in the unlikely case it was peak.
    if (lastValueWasAboveThrehold) {
        peaks.push(length - 1);
    }
    return peaks;
};
//# sourceMappingURL=/build/es2015/helpers/get-peaks-at-threshold.js.map